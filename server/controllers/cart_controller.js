module.exports = {

    add: (req, res, next ) => {
        const { id } = req.query;
       


    },

    delete: (req, res, next) => {
        const { id } = req.query;
    },
    
    update: (req, res, next) => {
        const { id } = req.query;
    }
    
}